CREATE TABLE "RedGate".orders (
  order_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
  customer_id NUMBER(6) NOT NULL,
  status VARCHAR2(20 BYTE) NOT NULL,
  salesman_id NUMBER(6),
  order_date DATE NOT NULL,
  PRIMARY KEY (order_id),
  CONSTRAINT fk_orders_customers FOREIGN KEY (customer_id) REFERENCES "RedGate".customers (customer_id) ON DELETE CASCADE,
  CONSTRAINT fk_orders_employees FOREIGN KEY (salesman_id) REFERENCES "RedGate".employees (employee_id) ON DELETE SET NULL
);